# Para o app no celular (Expo Go) conseguir falar com a API, use o IP do seu PC (ipconfig no Windows).
# A porta deve ser a mesma da API (8080).
# Após alterar este arquivo, reinicie o Expo (npm start).
EXPO_PUBLIC_API_URL=http://192.168.1.10:8080/api
